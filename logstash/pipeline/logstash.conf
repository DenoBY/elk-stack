input {
  beats {
    port => 5044
  }
}

filter {
  # Nginx access logs
  if [type] == "nginx-access" {
    grok {
      match => { "message" => "%{IPORHOST:client_ip} - %{DATA:user} \[%{HTTPDATE:timestamp}\] \"%{WORD:method} %{DATA:request} HTTP/%{NUMBER:http_version}\" %{NUMBER:status} %{NUMBER:bytes} \"%{DATA:referrer}\" \"%{DATA:user_agent}\"" }
    }
    date {
      match => [ "timestamp", "dd/MMM/yyyy:HH:mm:ss Z" ]
    }
    geoip {
      source => "client_ip"
      target => "geoip"
    }
  }

  # Nginx error logs
  if [type] == "nginx-error" {
    grok {
      match => { "message" => "%{DATA:timestamp} \[%{DATA:level}\] %{NUMBER:pid}#%{NUMBER:tid}: %{GREEDYDATA:error_message}" }
    }
  }

  # PHP-FPM logs
  if [type] == "php-fpm" {
    grok {
      match => { "message" => "\[%{DATA:timestamp}\] %{DATA:level}: %{GREEDYDATA:error_message}" }
    }
  }

  # Laravel logs
  if [type] == "laravel" {
    grok {
      match => { "message" => "\[%{TIMESTAMP_ISO8601:timestamp}\] %{DATA:env}\.%{DATA:level}: %{GREEDYDATA:laravel_message}" }
    }
    date {
      match => [ "timestamp", "yyyy-MM-dd HH:mm:ss" ]
    }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "%{type}-%{+YYYY.MM.dd}"
    user => "elastic"
    password => "${ELASTIC_PASSWORD}"
  }
}
